version: 2

deploy:
  steps:
    setupVariables:
      after:
        - name: "setupVariables:after"
          run: |
            echo "ENV0_STEP_NAME=${ENV0_STEP_NAME}"
            echo "ENV0_STEP_STATUS=${ENV0_STEP_STATUS}"
    terraformInit:
      before:
        - name: "terraformInit:before"
          run: |
            echo "ENV0_STEP_NAME=${ENV0_STEP_NAME}"
            echo "ENV0_STEP_STATUS=${ENV0_STEP_STATUS}"
      after:
        - name: "terraformInit:after"
          run: |
            echo "ENV0_STEP_NAME=${ENV0_STEP_NAME}"
            echo "ENV0_STEP_STATUS=${ENV0_STEP_STATUS}"
    cloudToCode:
      before:
        - name: "cloudToCode:before"
          run: |
            echo "ENV0_STEP_NAME=${ENV0_STEP_NAME}"
            echo "ENV0_STEP_STATUS=${ENV0_STEP_STATUS}"
      after:
        - name: "cloudToCode:after"
          run: |
            echo "ENV0_STEP_NAME=${ENV0_STEP_NAME}"
            echo "ENV0_STEP_STATUS=${ENV0_STEP_STATUS}"
            echo "ENV0_PULL_REQUEST_URL=${ENV0_PULL_REQUEST_URL}"
